<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow rounded-bottom mb-5">
        <a class="navbar-brand" href="{{ url('index') }}">Website</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('index') }}">Home <span class="sr-only"></span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('contact') }}">Contact <span class="sr-only"></span></a>
                </li>
            </ul>
            <span class="navbar-text">
                <ul class="navbar-nav">
                    {% if app.user %}
                        <li class="nav-item">
                           <span class="nav-link active">Welkom {{ app.user.name }}!</span>
                        </li>
                    {% if ('ROLE_ADMIN' in app.user.roles) or ('ROLE_SUPER_ADMIN' in app.user.roles) %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" href="#">Admin</a>
                            <div class="dropdown-menu text-dark dropdown-menu-right" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item text-dark" href="{{ url('admin_list') }}">Gebruikers</a>
                            </div>
                        </li>
                    {% endif %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('app_logout') }}">Logout</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('app_login') }}">Inloggen</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('app_registration') }}">Registreren</a>
                        </li>
                    {% endif %}
                </ul>
            </span>
        </div>
    </nav>
</header>